global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "agreements-api"
    static_configs:
      - targets:
          - "server:8080"
  
  - job_name: "generator-worker"
    static_configs:
      - targets:
          - "worker:8001"
diff --git a/config/local.yaml b/config/local.yaml
index 3d3dd37..2eb271b 100644
--- a/config/local.yaml
+++ b/config/local.yaml
@@ -13,7 +13,7 @@ storage:
   job_ttl: "5m"
   type: "postgres"
   driver: "postgres"
-  host: "host.docker.internal"
+  host: "db"
   port: "5432"
   database: "generator"
 redis:
diff --git a/docker-compose.yaml b/docker-compose.yaml
index 8f2b614..24488f5 100644
--- a/docker-compose.yaml
+++ b/docker-compose.yaml
@@ -10,22 +10,63 @@ services:
     build: .
     ports:
       - "8080:8080"
-      - "5432:5432"
     env_file:
       - .env
     volumes:
       - ./config/local.yaml:/root/config.yaml
     depends_on:
-      - worker
-      - redis
+      redis:
+        condition: service_healthy
+      db:
+        condition: service_healthy
+      migrations:
+        condition: service_completed_successfully
+        
+  db:
+    container_name: db
+    image: postgres:18-alpine
+    env_file:
+      - .env
+    ports:
+      - "5432:5432"
+    healthcheck:
+      test: ["CMD-SHELL", "pg_isready -U postgres"]
+      interval: 5s
+      timeout: 5s
+      retries: 5
+    volumes:
+      - postgres_data:/var/lib/postgresql/18/docker
+
+  migrations:
+    container_name: migrations
+    image: migrate/migrate:v4.19.1
+    command:
+      - "-path"
+      - "/migrations"
+      - "-database"
+      - "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?sslmode=disable"
+      - "up"
+    volumes:
+      - ./internal/storage/postgres/migrations:/migrations
+    depends_on:
+      db:
+         condition: service_healthy
   
   redis:
     container_name: redis
     image: redis:7-alpine
-    ports:
-      - "6379-6379"
     env_file:
       - .env
+    ports:
+      - "6379:6379"
+    healthcheck:
+      test: [ "CMD", "redis-cli", "ping" ]
+      interval: 5s
+      timeout: 3s
+      retries: 5
     command: redis-server --requirepass ${REDIS_PASSWORD}
     restart: unless-stopped
+
+volumes:
+  postgres_data:
     
diff --git a/internal/storage/postgres/create_tables.sql b/internal/storage/postgres/create_tables.sql
deleted file mode 100644
index 056b6aa..0000000
--- a/internal/storage/postgres/create_tables.sql
+++ /dev/null
@@ -1,28 +0,0 @@
-CREATE TABLE users (
-	id SERIAL PRIMARY KEY,
-	login TEXT UNIQUE NOT NULL,
-	name TEXT NOT NULL,
-	password BYTEA NOT NULL,
-	created_at TIMESTAMP DEFAULT NOW(),
-	updated_at TIMESTAMP DEFAULT NOW()
-);
-
-CREATE TABLE jobs (
-	id TEXT PRIMARY KEY,
-	status TEXT CHECK (status IN ('processing', 'completed', 'failed')),
-	user_id SERIAL REFERENCES users(id) ON DELETE CASCADE,
-	created_at TIMESTAMP DEFAULT NOW(),
-	updated_at TIMESTAMP DEFAULT NOW()
-);
-
-CREATE TABLE archives (
-	id SERIAL PRIMARY KEY,
-	job_id TEXT UNIQUE NOT NULL REFERENCES jobs(id) ON DELETE CASCADE,
-	archive BYTEA,
-	gen_count INTEGER CHECK (gen_count >= 0),
-	gen_errors JSONB,
-	fatal_gen_error TEXT,
-	created_at TIMESTAMP DEFAULT NOW(),
-	updated_at TIMESTAMP DEFAULT NOW()
-);
-
diff --git a/internal/storage/postgres/migrations/000001_init.down.sql b/internal/storage/postgres/migrations/000001_init.down.sql
new file mode 100644
index 0000000..365a210
--- /dev/null
+++ b/internal/storage/postgres/migrations/000001_init.down.sql
@@ -0,0 +1 @@
+DROP TABLE IF EXISTS users;
\ No newline at end of file
diff --git a/internal/storage/postgres/migrations/000001_init.up.sql b/internal/storage/postgres/migrations/000001_init.up.sql
new file mode 100644
index 0000000..6c302d0
--- /dev/null
+++ b/internal/storage/postgres/migrations/000001_init.up.sql
@@ -0,0 +1,28 @@
+CREATE TABLE IF NOT EXISTS users (
+                       id SERIAL PRIMARY KEY,
+                       login TEXT UNIQUE NOT NULL,
+                       name TEXT NOT NULL,
+                       password BYTEA NOT NULL,
+                       created_at TIMESTAMP DEFAULT NOW(),
+                       updated_at TIMESTAMP DEFAULT NOW()
+);
+
+CREATE TABLE IF NOT EXISTS jobs (
+                      id TEXT PRIMARY KEY,
+                      status TEXT CHECK (status IN ('processing', 'completed', 'failed')),
+                      user_id SERIAL REFERENCES users(id) ON DELETE CASCADE,
+                      created_at TIMESTAMP DEFAULT NOW(),
+                      updated_at TIMESTAMP DEFAULT NOW()
+);
+
+CREATE TABLE IF NOT EXISTS archives (
+                          id SERIAL PRIMARY KEY,
+                          job_id TEXT UNIQUE NOT NULL REFERENCES jobs(id) ON DELETE CASCADE,
+                          archive BYTEA,
+                          gen_count INTEGER CHECK (gen_count >= 0),
+                          gen_errors JSONB,
+                          fatal_gen_error TEXT,
+                          created_at TIMESTAMP DEFAULT NOW(),
+                          updated_at TIMESTAMP DEFAULT NOW()
+);
+
